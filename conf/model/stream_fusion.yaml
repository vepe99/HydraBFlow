# Stream model: fusion summary network (particle set + rotation curve) + diffusion posterior,
# as in the reference project's agama/rotationcurve models.
defaults:
  - override summary_network: stream_fusion
  - override inference_network: diffusion
  - _self_

name: stream_fusion
